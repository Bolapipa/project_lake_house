resources:
  pipelines:
    carros_pipeline:
      name: 'Carros ETL Pipeline'
      catalog: ${var.silver_catalog}
      schema: ${var.schema}
      
      libraries:
        - notebook:
            path: ../src/dlt_carros.sql
      
      configuration:
        source_catalog: ${var.catalog}
        source_schema: ${var.schema}
      
      serverless: true
      continuous: false
      development: ${var.pipeline_development}
      photon: true
      channel: current
      
      permissions:
        - level: CAN_VIEW
          group_name: 'users'
